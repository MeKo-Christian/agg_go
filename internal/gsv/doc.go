// Package gsv ports AGG's embedded vector-font text renderer.
//
// The package exposes the classic GSV text path generator used as a fallback or
// lightweight font path source when a full TrueType engine is unavailable.
package gsv
