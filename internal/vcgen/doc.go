// Package vcgen ports AGG's vertex generators.
//
// Vertex generators consume source paths and emit derived geometry such as
// strokes, contours, dashes, clipping results, and spline approximations before
// those vertices enter converters or rasterizers.
package vcgen
