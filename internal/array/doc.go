// Package array ports AGG's low-level container utilities.
//
// It provides pod-style arrays and vectors, block allocators, vertex sequences,
// and small generic algorithms used throughout the rendering pipeline where the
// original C++ code relied on agg_array.h helpers.
package array
